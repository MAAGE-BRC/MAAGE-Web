<!DOCTYPE html>
<html lang="en">
  <head>
    <title>dashboard_layout | MAAGE</title>
    <%- include('../head') %>
  </head>
      <body class="claro patric bg-maage-bg <% if (request.isAuthenticated && request.isAuthenticated()) { %>Authenticated<% } %>">
    <div id="ApplicationContainer" data-dojo-type="dijit/layout/BorderContainer" class="layoutContainer" data-dojo-props="id:'ApplicationContainer',gutters:false,liveSplitters:false">
      <%- include('../navbar') %>
      <div data-dojo-type="dijit/layout/ContentPane" data-dojo-props="region:'center'" style="padding:0px;border:0px solid #ddd;background:inherit;">
        <div id="bv-brc-page">
          <div id="bv-brc-page-container" style="width:100%;">

    <main class="flex-1 p-8 space-y-4">
      <section class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6">Metric 1</div>
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6">Metric 2</div>
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6">Metric 3</div>
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6">Metric 4</div>
      </section>

      <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6 h-64 flex items-center justify-center">Chart 1</div>
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6 h-64 flex items-center justify-center">Chart 2</div>
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6 h-64 flex items-center justify-center">Chart 3</div>
      </section>

      <section class="grid grid-cols-1 lg:grid-cols-3 gap-4">
        <div class="lg:col-span-2 bg-white shadow border border-gray-200 rounded-xl p-6 h-96 flex items-center justify-center">
          Map Placeholder
        </div>
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6 h-96 flex items-center justify-center">
          Chart 4
        </div>
      </section>

      <section class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6 h-64 flex items-center justify-center">Chart 5</div>
        <div class="bg-white shadow border border-gray-200 rounded-xl p-6 h-64 flex items-center justify-center">Chart 6</div>
      </section>
    </main>

          </div>
        </div>
      </div>
      <%- include('../footer') %>
    </div>
    <%- include('../javascript') %>
  </body>
</html>